struct EmbeddingTest
  @test "embedding_text leads with the entity name and type"
  fn test_shape ! String
    actual = embedding_text("Koja", "language", "compiled to native")

    if actual != "Koja (language): compiled to native"
      fail "got: " <> actual
    end
  end
end
